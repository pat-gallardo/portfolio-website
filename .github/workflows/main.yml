name: Build and Deploy

on:
  push:
    branches:
      - server-dev

permissions:
  contents: write

jobs:
  build:
    name: Build the Project
    runs-on: windows-latest
    steps:
      # Step 1: Checkout the repository
      - name: Checkout Code 🛎️
        uses: actions/checkout@v4

      # Step 2: Install dependencies and build the project
      - name: Install Dependencies and Build 🔧
        run: |
          cd frontend
          npm ci --force
          npm run build

      # Step 3: Upload build folder as an artifact
      - name: Upload Build Artifacts 🔺
        uses: actions/upload-artifact@v4
        with:
          name: site
          path: frontend/build # Ensure this matches the actual build output path

  deploy:
    name: Deploy to GitHub Pages
    needs: build
    runs-on: ubuntu-latest
    steps:
      # Step 1: Checkout the repository
      - name: Checkout Code 🛎️
        uses: actions/checkout@v4

      # Step 2: Download the build artifacts
      - name: Download Build Artifacts 🔻
        uses: actions/download-artifact@v4
        with:
          name: site

      # Step 3: Validate the artifact path
      - name: Validate Artifact Path
        run: |
          ls -la
          ls -la site

      # Step 4: Deploy to GitHub Pages
      - name: Deploy to GitHub Pages 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: 'site'
